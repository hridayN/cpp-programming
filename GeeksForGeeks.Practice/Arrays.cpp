#include <iostream>
using namespace std;

void ArrayRotation() {
	/*Problem: The first line of the input contains T denoting the number of testcases.
	First line of each test case contains two space separated elements, N denoting the size of the array and an integer D denoting the number size of the rotation. 
	Subsequent line will be the N space separated array elements.*/

	int testCases;
	cin >> testCases;

	for (int i = 1; i <= testCases; i++)
	{
		unsigned int N, D;	
		cin >> N;
		cin >> D;
		int arr[N];
	}
}

int main() {
	cout << "hello";
	
	return 0;
}